---
- name: Setup Kubernetes resource files for {{ name }}
  template:
    src: "../templates/{{ template }}-{{ item }}.yml"
    dest: "{{ yamls }}/{{ template }}-{{ item }}.yml"
  with_items: "{{ files }}"
